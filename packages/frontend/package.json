{
  "name": "frontend",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "dev": "parcel index.html -p 3000",
    "lint": "eslint . --ext ts",
    "tsc": "tsc --pretty --noEmit"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "types": "^1.1.0"
  }
}
